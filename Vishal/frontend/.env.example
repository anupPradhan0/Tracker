# Vite dev proxy target (must match backend PORT). Default: http://localhost:5000
VITE_API_PROXY_TARGET=http://localhost:5000
# Optional: call API directly (cross-origin). Unset in dev to use the proxy instead.
# VITE_API_URL=http://localhost:5000
