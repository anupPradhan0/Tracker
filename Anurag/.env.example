# Database (Postgres via Docker — keep POSTGRES_* in sync with DATABASE_URL)
POSTGRES_USER=anurag
POSTGRES_PASSWORD=anurag_dev
POSTGRES_DB=expense_tracker
POSTGRES_PORT=5433
DATABASE_URL="postgresql://anurag:anurag_dev@localhost:5433/expense_tracker?schema=public"

# Server
PORT=4000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# JWT
JWT_ACCESS_SECRET=change-me-access-secret-min-32-chars
JWT_REFRESH_SECRET=change-me-refresh-secret-min-32-chars
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Encryption (32-byte base64 for AES-256-GCM)
ENCRYPTION_KEY=change-me-32-byte-base64-key-here==

# Email (SMTP)
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=
MAIL_PASSWORD=
MAIL_FROM="Expense Tracker <noreply@example.com>"

# AI models (API keys are saved per user in app Settings)
GEMINI_MODEL=gemini-2.0-flash
COHERE_MODEL=command-r-plus-08-2024

# Docker
# - Postgres only: `docker compose up -d`
# - Full stack:      `pnpm docker:up:all`  (postgres + api + web)
# - Host dev uses DATABASE_URL above; compose `app` profile overrides DB host to `postgres`
# - For `app` profile, set CLIENT_URL to the web URL (default http://localhost:5173)
WEB_PORT=5173

# Frontend (host `pnpm dev` — direct API URL)
VITE_API_URL=http://localhost:4000/api/v1
# Docker web image bakes VITE_API_URL=/api/v1 and nginx proxies /api → api:4000
